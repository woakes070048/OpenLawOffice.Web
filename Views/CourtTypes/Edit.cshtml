@model OpenLawOffice.Web.ViewModels.Matters.CourtTypeViewModel

@{ 
    Layout = "~/Views/Shared/_NewLayout.cshtml";
    ViewBag.Title = "Edit Court Type";
    ViewBag.PageTitle = "Edit Court Type";
}

@section Breadcrumb {
    <ul class="breadcrumb">
        <li><a href="/Settings">Settings</a></li>
        <li><a href="/CourtTypes">Court Types</a></li>
        <li>Court Type: <a href="/CourtTypes/Details/@(Model.Id)">@(Model.Title)</a></li>
        <li><a href="/CourtTypes/Edit/@(Model.Id)" class="active">Edit</a></li>
    </ul>
}

@using (Html.BeginForm("Edit", "CourtTypes", FormMethod.Post, new { @class = "form-horizontal", @role = "form" }))
{
    @(Html.ValidationSummary(true))

    <div class="panel panel-default">
        <div class="panel-heading">Court Type Information</div>
        <div class="panel-body">
            <div class="form-group">
                <span class="control-label col-sm-2">Title<span class="required-field" title="Required Field">*</span></span>
                <div class="col-sm-10" style="padding-top: 7px;">
                    @(Html.TextBoxFor(m => m.Title, new { @class = "form-control" }))
                </div>
            </div>
            <div class="form-group">
                <div class="col-sm-offset-2 col-sm-10">
                    <button type="submit" class="btn btn-default">Submit</button>
                </div>
            </div>
        </div>
    </div>
}